<!DOCTYPE html>
<html>
  <title>Students Data</title>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <head>

  </head>

  <body>
    @yield('menu')
    @yield('table')
    @yield('reactjs')
    @yield('bootstrap')
    @yield('onejs')
  </body>
</html>
